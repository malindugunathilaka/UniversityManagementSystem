<Window x:Class="UniversityManagementSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UMS Login" Height="300" Width="400" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Background="LightBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Border Grid.Row="0" Background="DarkBlue" Padding="10">
            <TextBlock Text="University Management System" 
                       Foreground="White" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Login Form -->
        <Grid Grid.Row="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
                <Label Content="Username:" FontWeight="Bold" FontSize="14"/>
                <TextBox x:Name="UsernameTextBox" Margin="0,0,0,15" Height="30" FontSize="12"/>

                <Label Content="Password:" FontWeight="Bold" FontSize="14"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,25" Height="30" FontSize="12"/>

                <Button x:Name="LoginButton" 
                        Content="Login" 
                        Height="35" 
                        FontSize="14"
                        FontWeight="Bold"
                        Background="DarkBlue"
                        Foreground="White"
                        Click="LoginButton_Click" 
                        IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>